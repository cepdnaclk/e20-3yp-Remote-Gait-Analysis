spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}  # Defaults to dev if not set

logging:
  level:
    org.springframework.boot.context.config: DEBUG


#client:
#  domains:
#        list:
#          - apple
#          - banana


#logging:
#  level:
#    org:
#      springframework: DEBUG
#      hibernate.SQL: DEBUG
#    com:
#      _yp:
#        gaitMate: DEBUG